# CalcNova 2.9.5 Release Checkpoint

Release preparation date: **2026-08-24**

## Identity

- Product/display version: `2.9.5`
- .NET/NuGet version: `2.9.5`
- Release tag: `v2.9.5`
- Assembly/file version: `2.9.5.0`
- Android/iOS build code: `20905`
- Application id: `in.sanskar.calcnova`

## Scope

CalcNova 2.9.5 is the completed 2.9-series source/release baseline prepared after the preserved 2.9.0 checkpoint. It retains the existing calculator feature set and maintained Desktop, Browser/WebAssembly/PWA, Android, and iOS architecture while strengthening release identity, source validation, documentation consistency, and completion workflow behavior.

Prepared source changes include:

- centralized SDK-independent release identity parsing in `tools/release_identity.py`;
- deterministic release tag, assembly/file version, and mobile build-code derivation;
- packaging, completion-status, platform-support, and release-document validators derived from current central release identity instead of hardcoded 2.8.03 constants;
- regression coverage for current mobile build identity and release-document identity;
- importlib-safe release identity helper behavior;
- Android/iOS build code `20905`;
- in-app About identity `Version 2.9.5 • Complete` and compiled/headless regressions;
- Linux AppStream 2.9.5 stable metadata while preserving 2.9.0 and 2.8.03 release entries;
- release-neutral `CalcNova Current Release Completion Validate` workflow with release-identity regression execution and superseded-run cancellation;
- synchronized README, project state, security/support policy, contributor guide, documentation index, feature inventory, roadmap, versioning, platform support, Source Preflight, release process, release evidence checklist, changelog, and live handoff;
- maintained x64/ARM64 Desktop release targets for Windows/Linux/macOS;
- maintained Browser/WebAssembly/PWA source composition;
- maintained explicit Android and iOS runtime identifier contracts;
- existing NuGet Audit, CodeQL, Dependency Review, CycloneDX SBOM, checksum, provenance, and structured release-evidence infrastructure.

## Evidence boundary

This checkpoint records source/release preparation. It does not claim unobserved hosted CI, compiled .NET validation, package launch, Browser runtime behavior, physical-device behavior, signing, notarization, TestFlight/App Store, Play Console, or release-publication PASS results.

Use `PASS`, `FAIL`, `BLOCKED`, or `NOT RUN` only for operations that actually execute and are observed.

## Supersession

The repository is intentionally advanced from this checkpoint to CalcNova **2.9.6** in the subsequent maintenance pass. This file remains the historical record of the completed 2.9.5 source/release baseline.
